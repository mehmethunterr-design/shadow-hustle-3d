extends Node

# The playable landscape is now built and owned by scripts/world.gd.
# Keep this autoload as a harmless compatibility stub so existing project
# settings and saved scenes do not break.
func _ready() -> void:
	pass
